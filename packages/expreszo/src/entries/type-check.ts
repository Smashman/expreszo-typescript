/**
 * `@pro-fa/expreszo/type-check` entry — runtime type-check predicates
 * (`isArray`, `isNumber`, …).
 */
export { withTypeCheck } from '../api/presets.js';
export { TYPE_CHECK_FUNCTIONS } from '../registry/presets/type-check.js';
