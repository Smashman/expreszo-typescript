/**
 * `@pro-fa/expreszo/utility` entry — `if`, `json`, and the `as` cast operator.
 */
export { withUtility } from '../api/presets.js';
export { UTILITY_OPERATORS, UTILITY_FUNCTIONS } from '../registry/presets/utility.js';
