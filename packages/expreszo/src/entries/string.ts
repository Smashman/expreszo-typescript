/**
 * `@pro-fa/expreszo/string` entry — string manipulation functions plus the
 * `length` unary prefix.
 */
export { withString } from '../api/presets.js';
export { STRING_OPERATORS, STRING_FUNCTIONS } from '../registry/presets/string.js';
