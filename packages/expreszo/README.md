# @pro-fa/expreszo

The canonical README for this package lives at the repository root: [`README.md`](https://github.com/Pro-Fa/expreszo-typescript/blob/main/README.md).

The publish workflow copies it into this directory before running `npm publish`, so the version on the npm registry is always in sync. If you're seeing this stub in a published tarball, the copy step misfired — please open an issue.
